# Grafana auto-discovers this file on startup and pre-configures
# Prometheus as a datasource. No manual setup needed.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
